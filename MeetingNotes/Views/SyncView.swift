import SwiftUI

struct SyncView: View {
    var body: some View {
        Text("Syncing View...")
    }
}

struct SyncView_Previews: PreviewProvider {
    static var previews: some View {
        SyncView()
    }
}
